# DATABASE_URL (required)
DATABASE_URL=postgresql://user:password@localhost:5432/mubawab

# Optional: separate read/write replicas
# DATABASE_URL_READ=postgresql://user:password@replica:5432/mubawab

# Optional: PostgreSQL SSL
# PG_SSL=true

# Optional: HTTP server port (default: 3001)
# PORT=3001
